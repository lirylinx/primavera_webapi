import 'package:primavera_webapi/src/model/artigo/artigo.dart';
import 'package:primavera_webapi/src/utils/fetch.dart';

Future<void> main(List<String> args) async {
  //     final Utilizador utilizador =
  //       Utilizador(username: username, password: senha);
  // Fetch().getToken().then((data) async {
  // await Fetch().criarEncomenda(
  //     data.accessToken, 'ecw', 2023, 'ADA001', DateTime.now().toString(), [
  //   Artigo(artigo: '002', quantidade: 2),
  //   Artigo(artigo: '00251', quantidade: 5)
  // ]);

  // await Fetch().abrirPosCaixa(data.accessToken);
  // await Fetch().fecharPosCaixa(data.accessToken, 'cm1');
  // });
}
